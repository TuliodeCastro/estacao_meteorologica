# Configuração do Cloudflare Worker (proxy do Meteorinho → NVIDIA)
name = "meteorinho-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# A chave da NVIDIA NÃO fica aqui! Ela é um "secret", definido por:
#   npx wrangler secret put NVIDIA_API_KEY
# (veja o README.md desta pasta)
